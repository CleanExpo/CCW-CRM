name: CCW Boardroom CI

on:
  push:
    branches: [main, develop, fix/railway-cache-auth-500]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    name: Test Suite
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Run unit tests
        run: npm run test

  validate:
    name: Validation Gates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Validate hooks structure
        run: node scripts/ci/validate-hooks.js
      - name: Validate agent YAML frontmatter
        run: node scripts/ci/validate-agents.js
      - name: Validate CRON job registrations
        run: node scripts/ci/validate-cron-jobs.js
      - name: Validate vercel.json crons resolve to live handlers
        run: |
          node scripts/ci/validate-vercel-crons.js
          node scripts/ci/validate-vercel-crons.js --self-test
      - name: Validate every stylesheet is reachable
        run: |
          node scripts/ci/validate-css-sources.js
          node scripts/ci/validate-css-sources.js --self-test
      - name: Validate Deepsec workflow contract
        run: |
          node scripts/ci/validate-deepsec-workflow.js
          node scripts/ci/validate-deepsec-workflow.js --self-test

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Audit dependencies
        run: npm audit --audit-level=high || true
      - name: Scan for hardcoded secrets
        run: node scripts/ci/scan-secrets.js

  # The browser suite was written but never wired into CI, so an accessibility
  # regression would not have surfaced until a human happened to run it locally.
  #
  # This runs against THE CANDIDATE, not production. An earlier revision of this
  # job set no E2E_BASE_URL, so playwright.config.ts fell back to the production
  # alias — meaning a pull request was audited against code it had not changed.
  # A dev server is started here instead; the public pages need no database.
  #
  # Accessibility and console-error specs only, on purpose. The visual-baseline
  # specs compare against six PNGs captured from PRODUCTION, which a local dev
  # render will not match pixel-for-pixel, so including them here would produce a
  # permanent red for a non-reason. Visual comparison stays a manual check until
  # the baselines are regenerated from a stable target — tracked in
  # docs/design-system.md.
  #
  # The authenticated specs need credentials and a working database; see
  # docs/RESTORE-PRODUCTION-DATABASE.md.
  e2e-public:
    name: Browser Suite (public routes, accessibility)
    runs-on: ubuntu-latest
    # A process that accepts a connection but never completes the response would
    # otherwise block inside one curl and hang the job past its nominal 60-try
    # limit. Fail, do not hang.
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      # Next dev silently moves to another port when 3000 is taken. That exact
      # failure produced a measurement against an unrelated application during
      # development of this branch — the page looked fine because it was
      # somebody else's page. Read the port Next actually bound, never assume it.
      - name: Start the candidate
        run: |
          JWT_SECRET=ci-ephemeral-not-a-real-secret npm run dev > /tmp/dev.log 2>&1 &
          PORT=""
          for i in $(seq 1 60); do
            PORT=$(grep -oE 'http://localhost:[0-9]+' /tmp/dev.log | head -1 | grep -oE '[0-9]+$' || true)
            if [ -n "$PORT" ] && curl -sf --connect-timeout 5 --max-time 20 -o /dev/null "http://localhost:$PORT/"; then
              echo "dev server ready on port $PORT"
              echo "E2E_BASE_URL=http://localhost:$PORT" >> "$GITHUB_ENV"
              exit 0
            fi
            sleep 2
          done
          echo "dev server never became ready"; cat /tmp/dev.log; exit 1
      - name: Confirm the target is this application
        run: |
          TITLE=$(curl -s --connect-timeout 5 --max-time 20 "$E2E_BASE_URL/" | grep -oE '<title>[^<]*</title>' || true)
          echo "target title: $TITLE"
          case "$TITLE" in
            *"CCW Online"*) echo "correct application" ;;
            *) echo "WRONG APPLICATION on $E2E_BASE_URL — refusing to report a result"; exit 1 ;;
          esac
      - name: Accessibility and console-error specs
        run: npx playwright test e2e/public-surface.spec.ts -g "accessibility|console"
      # Signup→onboarding needs ALLOW_PUBLIC_REGISTRATION=true, MFA_ENFORCE=false,
      # and a reachable database. This job has none of those. Run the spec here
      # only when those secrets exist so a missing DB fails that dedicated step
      # instead of skipping inside Playwright.
      - name: Signup onboarding journey
        if: ${{ env.E2E_SIGNUP_ENABLED == 'true' }}
        env:
          ALLOW_PUBLIC_REGISTRATION: 'true'
          MFA_ENFORCE: 'false'
        run: npx playwright test e2e/signup-onboarding.spec.ts --project=desktop
      - name: Upload report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

  # UNI-2108. Kept off the deploy gate until this job has passed on the candidate
  # three times — an unrehearsed required check would red main on first fire.
  smoke-erp:
    name: ERP smoke pack (UNI-2108)
    runs-on: ubuntu-latest
    timeout-minutes: 25
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: ccw_smoke
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/ccw_smoke
      JWT_SECRET: ci-ephemeral-not-a-real-secret
      JWT_SECRET_KEY: ci-ephemeral-not-a-real-secret
      MFA_ENFORCE: 'false'
      ALLOW_PUBLIC_REGISTRATION: 'false'
      E2E_EMAIL: smoke.owner@local.test
      E2E_PASSWORD: SmokeOwner12ab
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Apply migrations
        run: npx prisma migrate deploy
      - name: Seed smoke owner
        run: npm run erp:seed-smoke
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Start the candidate
        run: |
          npm run dev > /tmp/dev-smoke.log 2>&1 &
          PORT=""
          for i in $(seq 1 90); do
            PORT=$(grep -oE 'http://localhost:[0-9]+' /tmp/dev-smoke.log | head -1 | grep -oE '[0-9]+$' || true)
            if [ -n "$PORT" ] && curl -sf --connect-timeout 5 --max-time 20 -o /dev/null "http://localhost:$PORT/"; then
              echo "E2E_BASE_URL=http://localhost:$PORT" >> "$GITHUB_ENV"
              exit 0
            fi
            sleep 2
          done
          echo "dev server never became ready"; cat /tmp/dev-smoke.log; exit 1
      - name: Run ERP smoke
        run: npx playwright test e2e/erp-smoke.spec.ts --project=desktop
      - name: Upload smoke report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: erp-smoke-report
          path: playwright-report/
          retention-days: 7

  deploy:
    name: Deploy Gate
    needs: [test, validate, security, e2e-public]
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy gate passed
        run: echo "All gates passed. Vercel auto-deploy triggered on push to main."
