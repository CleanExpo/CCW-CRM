/**
 * BISECT STUB — Phase 2g diagnostic (UNI-1949)
 * Original page preserved in Claude session context,
 * will be restored incrementally based on 200/500 result.
 */

export default function Home() {
  return <div>Phase 2g bisect baseline — if you see this, the minimal layout works.</div>;
}
