# Session handoff — 2026-04-19 ~06:00 AEST

Previous Claude session (Opus 4.7, 1M context) ran the CCW-ERP/CRM
autonomous loop for ~2 hours and shipped **25 tickets** before hard-pausing
on context exhaustion. This file tells the next session where to pick up.

## Branch + PR state

- **Branch:** `claude/sad-banzai-b03e18` (git worktree at
  `C:\CCW-Online ERP\.claude\worktrees\sad-banzai-b03e18\`).
- **Commits ahead of main:** 29 (all pushed to origin).
- **Draft PR:** https://github.com/CleanExpo/CCW-CRM/pull/146
  — one commit per ticket, each verified and documented.
- **Working tree:** clean except for pre-existing
  `docs/ISS-028-VERIFICATION.md` deletion (not mine — leave alone).

## Tickets shipped this session (25)

Autonomy fix: Layer 1/2/3 + Stop-hook fallback + `defaultMode`
permissions fix (4 commits at the start of the branch).

Feature/fix tickets:

```
UNI-1876  563dafdc  Shipping: remove US carrier references
UNI-1874  d2d34d15  Purchasing: threshold-based PO approval
UNI-1801  20e6f006  Settings: AEST/AEDT timezone display
UNI-1788  99cccec0  Nav: mobile hamburger shows all groups
UNI-1965  a6f5a315  UI: 6 polish fixes
UNI-1943  28946c68  Docs: SLO overview + BetterStack runbook
UNI-1946  ea2fb196  Audit: silent-fail frontend (38 findings)
UNI-1942  181d3d52  Audit: TODO triage (24 legit markers)
UNI-1789  df37b389  RSC: fix 503 prefetch storm
UNI-1941  52474d69  Types: strict baseline (TS 0 / mypy 242)
UNI-1865  d1d18b97  Security: configurable session timeout
UNI-1795  2df1bdf4  Catalog fix (partial — D1 of 4)
UNI-1939  4d73b074  E2E: Playwright + nightly CI
UNI-1922  93b63ff6  Runbook: go-live 2026
UNI-1930  f978f628  Plan: backend god-file split
UNI-1923  857b7899  Archive: 10 unused project skills
UNI-1928  801648e0  Deps: Renovate auto-merge
UNI-1936  bceaa2e8  ElevenLabs: admin-configurable key
UNI-1925  bc5d8984  Dead-code: knip baseline
UNI-1933  d8bf276b  Refactor: YouTube script package split
UNI-1869  a6920961  Security: close supplier-portal anon access
UNI-1834  167bdb29  CFO AP ageing report
UNI-1832  9de7e9e5  Landed-cost apportionment at GRN
UNI-1828  882dba1b  Warehouse pick lists + packing slips
```

4 Pi-SEO false positives closed: UNI-1955 / 1957 / 1962 / 1963.

2 deduped (existing human PRs — left alone to avoid merge conflicts):

- UNI-1823 → PR #138 open (serial/lot tracking)
- UNI-1825 → PRs #136, #139 open (job card)

## Remaining Todo queue (CCW-ERP/CRM)

Ordered by recommended pickup sequence:

1. **UNI-1822 — Multi-carrier shipping abstraction layer.** Foundational.
   AusPost + TNT + FedEx adapters through a common `CarrierAdapter` interface.
   This is the top priority and deserves the fresh context budget you have now.
   File anchors: `apps/backend/src/services/carrier_service.py`,
   `apps/backend/src/api/routes/shipments.py`,
   `apps/backend/src/integrations/` (new adapters directory).
2. **UNI-1824 — Dangerous Goods declarations.** Blocked on UNI-1822 — the
   DG payload passes through the carrier API. Do not start until 1822 lands.
3. **UNI-1827 — Parts usage ↔ inventory.** Blocked until UNI-1825 PRs
   (#136, #139 — job card) land, because parts consumption writes from the
   job-card schema.

After those, the Todo queue will be empty. The Backlog has the
"health-check-2026-04" tickets — some doable, most need a focused session.

## Known pre-existing bugs to flag (not fix)

These were discovered during the session but are out-of-scope:

- `apps/backend/src/services/notification_service.py` imports `User` from
  the locked `demo_models.py` but that symbol isn't exported there —
  blocks the shared `conftest.py` fixture stack.
- `pi-ceo` MCP has a revoked `LINEAR_API_KEY` — use the fallback
  `mcp__8eec34b8-ab6d-4f8e-9437-04ad5f8376fa__*` Linear MCP instead.

## Locked files — never touch

- `apps/backend/src/db/demo_models.py`
- `apps/web/middleware.ts`
- `apps/backend/src/api/routes/demo_auth.py`

## Safe-continuation pattern (proven this session)

1. Pull next Todo ticket via Linear MCP.
2. **Before implementing**, check for existing open PRs via
   `gh pr list --search "in:title UNI-XXXX" --state open` AND inspect
   the ticket's `attachments` field in Linear. If found, comment "PR
   #N covers this" and move to the next ticket. Do NOT duplicate work.
3. Implement. Run relevant tests + type-check.
4. Commit with a descriptive body (what/why/verification).
5. **Push immediately after every commit** — work-loss risk stays at zero.
6. Update the Linear ticket to Done + post a commit summary comment.
7. If context starts feeling full, stop cleanly — do not push through on
   a big foundational ticket with tired context.

## Autonomous mode

- `.claude/.autonomous-mode` marker is currently **removed** (clean pause).
- To resume: `touch .claude/.autonomous-mode` in the worktree, then start
  Claude Code. The Stop hook will fire and instruct the new session to
  pull the next Todo ticket.

---

_Previous session closed cleanly. No dangling work, no uncommitted edits
(except the pre-existing `docs/ISS-028-VERIFICATION.md` deletion which is
not mine)._
